# 🤖 AI Console Assistant

Умный консольный помощник для Linux, работающий с локальным сервером **Ollama**. 
Ассистент генерирует bash-команды по вашему описанию, объясняет их действие и позволяет выполнить их одним нажатием Enter.

## ✨ Особенности

*   **Генерация команд:** Превращает естественный язык ("как найти большие логи") в готовые bash-команды.
*   **Безопасность:** Показывает команду и пояснение *перед* выполнением. Вы можете отредактировать её или отменить запуск.
*   **Умный выбор моделей:** Автоматически определяет, какая модель лучше подходит для задач (код или чат). Поддерживает переключение между моделями "на лету".
*   **Гибкая настройка:** Управление адресом сервера Ollama и выбором моделей через ключи командной строки.
*   **Локальность:** Все данные обрабатываются на вашем сервере, ничего не уходит в облако.
*   **Редактирование на лету:** Встроенный редактор позволяет поправить команду перед запуском (например, изменить дату или путь).

## 🚀 Установка

### Требования
*   Linux (Debian, Ubuntu, AstraLinux, Armbian и др.)
*   Установленный и запущенный сервер [Ollama](https://ollama.com/)
*   Утилиты: `curl`, `jq`

### Быстрая установка

1.  Скачайте скрипт и сделайте его исполняемым:
    ```bash
    sudo curl -L https://raw.githubusercontent.com/sirius-parallax/ai-assist/main/ai -o /usr/local/bin/ai
    sudo chmod +x /usr/local/bin/ai
    ```
    *(Замените ссылку на актуальную после публикации)*

2.  Установите зависимости (если нет):
    ```bash
    sudo apt install curl jq
    ```

3.  Проверьте установку:
    ```bash
    ai -h
    ```

## ⚙️ Настройка

При первом запуске скрипт создаст конфиги в домашней директории (`~/.ai_assistant.conf` и `~/.ai_config`).

### Подключение к серверу Ollama
Если ваш сервер Ollama находится на другом компьютере или использует нестандартный порт:

```bash
ai --set-host http://192.168.1.100:11434

Вот краткая и понятная инструкция по установке твоего `.deb` пакета. Ты можешь добавить её в конец `README.md` или использовать как отдельный файл `INSTALL.md`.

---

# 📦 Установка пакета ai-assistant

Этот пакет предназначен для дистрибутивов на базе Debian (Debian, Ubuntu, Astra Linux, Armbian и др.).

### 1. Требования
Перед установкой убедитесь, что у вас есть:
*   Права суперпользователя (`sudo`).
*   Подключенные репозитории системы.

### 2. Установка
Откройте терминал в папке, где находится файл `ai-assistant-1.0.deb`, и выполните:

```bash
sudo apt install ./ai-assistant-1.0.deb
```

> **Почему `apt`, а не `dpkg`?**  
> Команда `apt install` автоматически проверит и установит необходимые зависимости (`curl` и `jq`), если они отсутствуют в вашей системе.

### 3. Проверка установки
После завершения установки проверьте работоспособность команды:

```bash
ai -h
```

Вы должны увидеть справку с текущими настройками.

### 4. Первая настройка
Если у вас еще не настроен сервер Ollama или вы хотите подключить удаленный сервер:

1.  Укажите адрес сервера:
    ```bash
    ai --set-host http://<IP-АДРЕС-СЕРВЕРА>:11434
    ```
2.  Обновите список моделей:
    ```bash
    ai --update
    ```
3.  Выберите модель по умолчанию:
    ```bash
    ai -m qwen2.5-coder:1.5b
    ```

### 5. Удаление
Если пакет вам больше не нужен, его можно удалить командой:

```bash
sudo apt remove ai-assistant
```

*(Конфигурационные файлы в домашней папке пользователя `~/.ai_assistant.conf` и `~/.ai_config` при этом сохранятся. Для полного удаления используйте `sudo apt purge ai-assistant`).*
